/**
 * Script-module dependency carrier for the ActivityPub app.
 *
 * PHP registers the runtime dependencies for this module so the import map
 * contains core boot plus all route modules before the inline boot call runs.
 */
