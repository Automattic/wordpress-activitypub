/**
 * Save function for Extra Fields block.
 *
 * This block uses server-side rendering, so the save function returns null.
 *
 * @return {null} No save content.
 */
export default function save() {
	return null;
}
