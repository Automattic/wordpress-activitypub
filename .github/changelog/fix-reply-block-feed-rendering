Significance: patch
Type: fixed

RSS and Atom feeds for posts that start with a reply block now show a simple `@username` mention instead of the full embed card, which only renders properly when the plugin's frontend CSS is loaded.
